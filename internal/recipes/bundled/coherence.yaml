id: coherence
title: Coherence
description: Run deterministic repository consistency checks before commits and handoffs.
surfaces:
  - doctor
  - init
  - precommit
commands:
  doctor:
    - args: ["coherence", "doctor", "--json"]
  precommit:
    - args: ["coherence", "scan", "--staged"]
