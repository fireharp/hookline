id: secrets-gitleaks
title: Secrets and Gitleaks
description: Check staged changes for literal local env values and generic secret patterns.
surfaces:
  - doctor
  - init
  - precommit
  - scan
commands:
  doctor:
    - args: ["gitleaks", "version"]
